# Copy to contrib/.env and fill in. Never commit contrib/.env.

# Postgres / Nominatim DB password (choose a long random value)
NOMINATIM_PASSWORD=

# Cloudflare Tunnel token for a dashboard-managed tunnel.
# Zero Trust -> Networks -> Tunnels -> Create a tunnel (Cloudflared) -> copy the token.
CF_TUNNEL_TOKEN=
